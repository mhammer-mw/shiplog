/** Single source of truth for app identity, surfaced in the UI and /api/health. */
export const APP_NAME = "Shiplog";

/** Kept in sync with package.json version. */
export const APP_VERSION = "0.1.0";
